#!/usr/bin/python3
def multiple_returns(sentence):
    """
    a function that returns a tuple with the length of a string and its first character.
    """
    for i in len(sentence) - 1:
      if len(sentence) == {}:
         sentence{} == None
      print("length : {d} sentence{0}".format(lelength, sentence))
